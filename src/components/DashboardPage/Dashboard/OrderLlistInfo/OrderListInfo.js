import React from 'react';
import './OrderListInfo.css';
const OrderListInfo = ({orderPd}) => {
    const{_id,name,email,prize,status,image}=orderPd;
    return (
           <h1></h1> 
    );
};

export default OrderListInfo;